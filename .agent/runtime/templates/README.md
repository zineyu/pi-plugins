# Agentic Task Runtime Templates

This directory holds project-local task artifact templates copied into `.agent/runtime/templates/`.

Concrete templates may be added here when the runtime needs reusable seed content beyond the defaults embedded in `task.py`.
